# =============================================================================
# MAGE CRAFT - Environment Variables Configuration
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control!

# -----------------------------------------------------------------------------
# Supabase Configuration (Required)
# -----------------------------------------------------------------------------
# Get these from your Supabase project dashboard
# https://supabase.com/dashboard/project/[your-project-id]/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anonymous_key_here

# -----------------------------------------------------------------------------
# LLM API Configuration (Required for AI-Enhanced Prompts)
# -----------------------------------------------------------------------------
# Choose your preferred LLM provider (openai or gemini)
# If not set, will try OpenAI first, then Gemini
NEXT_PUBLIC_LLM_PROVIDER=openai

# OpenAI Configuration (Recommended)
# Get your API key from https://platform.openai.com/api-keys
# Models: gpt-3.5-turbo (cheaper), gpt-4-turbo-preview (better)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google Gemini Configuration (Alternative - Free tier available)
# Get your API key from https://makersuite.google.com/app/apikey
# Models: gemini-pro (free tier available)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Specific model configuration
# OPENAI_MODEL=gpt-3.5-turbo
# GEMINI_MODEL=gemini-pro

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Base URL for the application (used for redirects and absolute URLs)
# For production, set this to your actual domain (e.g., https://your-app.vercel.app)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Environment (development, staging, production)
NODE_ENV=development

# Vercel URL (automatically set by Vercel, don't set manually)
# NEXT_PUBLIC_VERCEL_URL=

# -----------------------------------------------------------------------------
# Optional: Analytics and Monitoring
# -----------------------------------------------------------------------------
# Vercel Analytics (if using Vercel)
# VERCEL_ANALYTICS_ID=your_vercel_analytics_id

# -----------------------------------------------------------------------------
# Development Settings
# -----------------------------------------------------------------------------
# Set to true to enable debug logging
DEBUG=false

# Database connection pooling (for production)
# DATABASE_URL=your_direct_supabase_connection_string